const num1 = 10
const num2 = 5

function addValues() {
 console.log(`the sum is ${num1 + num2}`);
}

addValues()

/* sometimes to export a function, you basically dont need to write the export syntax you just need to invoke it/ call it */